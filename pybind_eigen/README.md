Installation is done with setuptools (using cmake underneath)
While developing, use
```
cd pymodule
python setup.py develop
```
NOTE: running the file from the parent folder will fail.

To uninstall use
```
python setup.py develop --uninstall
```

